policies:
  policy2:
    title: policy 2 description
    target:
      operation: OR
      conditions:
      - action-id == CourseUnit.Edit
      - action-id == CourseUnit.Delete
    rules:
      rule1:
        operation: OR
        conditions:
        - object.CourseUnit.author !=  user.personId
        - object.CourseUnit.status in DRAFT, TEST
      rule2:
        conditions:
        - object.CourseUnit.count > 15
        - object.CourseUnit.status not in DRAFT, APPROVED