policies:
  policy2:
    title: policy2
    target:
      operation: OR
      conditions:
      - action-id == CourseUnit.Edit
      - action-id == CourseUnit.Delete
    rules:
      rule1:
        operation: OR
        conditions:
        - not object.CourseUnit.author ==  user.personId
        - object.CourseUnit.status in (DRAFT, TEST)
      rule2:
        conditions:
        - object.CourseUnit.count > 15